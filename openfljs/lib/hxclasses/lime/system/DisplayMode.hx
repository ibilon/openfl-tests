package lime.system;

extern class DisplayMode {
	var height(default,null) : Int;
	var pixelFormat(default,null) : lime.graphics.PixelFormat;
	var refreshRate(default,null) : Int;
	var width(default,null) : Int;
}
